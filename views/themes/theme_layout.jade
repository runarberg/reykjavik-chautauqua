- var title = "Reykjavík Chautauqua"


doctype html
html
  head
    meta(charset="utf-8")
    title= theme.name
    block stylesheet
      link(rel='stylesheet', href='/stylesheets/themes.css')

  body
    header.main-header(class=theme.url.slice(1))
      block header
        h1= theme.name
        h2
          a(href="/")= title
        
        section.intro
          block intro
      
    section.posts(id="posts")
      h2 Posts
      each post in posts
        include ./post_layout.jade

    input(type="checkbox", rel="dropdown", id="dropdown-new-post")
    label(for="dropdown-new-post") New post
    section.new-post(id="new-post")
      article.preview
        header
          h2.title
        footer
          span.author
        section.content
        
      form#new-post-form(action=theme.url + "/new-post",
                         method="POST")
        input(type="text", name="title", placeholder="Title")
        div.form-content
          textarea(name="post", placeholder="Article")
          textarea(name="content", placeholder="Article")
          div.editmenu
            ul
              li
                button.heading2(value="h2", title="Second heading") H
              li
                button.heading3(value="h3", title="Third heading") H
              li
                button.strong(value="strong", title="Strong") B
              li
                button.em(value="em", title="Emphasize") I
              li
                button.anchor(value="a", title="Hyperlink") a
              li
                button.img(value="img", title="Image URL") img
              li
                button.yt(value="yt", title="You Tube") yt
              li
                button.vimeo(value="vimeo", title="Vimeo") vimeo

        input(type="text", name="author", placeholder="Author")
        input(type="submit", value="submit")

    footer.theme-nav
      ul.theme-list
        each theme_ in themes
          if theme_.name == theme.name
            li(class=(theme_.name == theme.name ? "active " : " ") +
               theme_.url.slice(1))
              a(href=theme_.url)
                p= theme_.dateStr
                p= theme_.name
          else
            li(class=theme_.url.slice(1))
              a(href=theme_.url)
                p= theme_.dateStr
                p= theme_.name

    script(src="/scripts/themes.js")